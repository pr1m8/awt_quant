# readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

python:
  install:
    - requirements: docs/requirements.txt
  system_packages: false

sphinx:
  configuration: docs/source/conf.py
